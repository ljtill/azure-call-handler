{
  "name": "call-handler",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rimraf dist",
    "prestart": "npm run clean && npm run build",
    "start": "func start",
    "test": "echo \"No tests yet...\""
  },
  "dependencies": {
    "@azure/communication-call-automation": "^1.0.0",
    "@azure/cosmos": "^4.0.0",
    "@azure/functions": "^4.0.1",
    "@azure/identity": "^3.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.8.4",
    "rimraf": "^5.0.5",
    "typescript": "^5.2.2"
  },
  "main": "dist/src/functions/*.js"
}